# Prefix with VITE_ to expose to the browser bundle.
# Leave blank in development — Vite proxies /api to the backend automatically.
# Set to the full backend URL in production (e.g. https://api.chatnet.app).
VITE_API_URL=
